#include "main.h"

/**
  * add - adds two integers and returns the result
  * @x: holds first integer to be added
  * @y: holds second integer to be added
  * Return: reuslt of addition
  */
int add(int x, int y)
{
	return (x + y);
}
