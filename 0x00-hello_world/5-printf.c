#include <stdio.h>

/**
 * main - entry point
 *
 * Description: program prints out a single line output
 * Return: always 0 (success)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
